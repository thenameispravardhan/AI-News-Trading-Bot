================================================================================
                     AI NEWS TRADING BOT — PROJECT OVERVIEW
================================================================================

Project name:    AI News Trading Bot  (UI brand: "TRADEBOT")
Version:         1.0.0        (app/__init__.py __version__; FastAPI app version)
                 NOTE: config.py still carries an internal APP_VERSION="0.1.0"
                 field and frontend/package.json is at 0.1.0 — the authoritative
                 app version is app.__version__ = "1.0.0".
Workspace:       C:\Projects\AI News Trading Bot
Platform:        Windows (PowerShell scripts) + Linux/macOS (bash scripts)
Genre:           Local-first, AI-driven, intraday equity news-trading system
Markets:         Indian equities (NSE / BSE), cash intraday (MIS) only
Broker:          Fyers (live orders + realtime feeds); built-in paper simulator
LLM:             DeepSeek (news classification / fact extraction)
Audience:        Single local operator. Binds to 127.0.0.1, NO authentication.

This file is a full, current description of the system as it exists in the
repository. Where the running defaults differ from the shipped `.env.example`
template, both are called out (see §11).

Last major update: 2026-07-10 — entry state machine (EntryManager +
RetracementMonitor), realtime tick-driven exit management with broker-routed
exits (paper AND Fyers), breakeven/consolidation/stall exit rules, position
reconciliation (CLOSED_EXTERNAL), direction-aware stop validation, the
customizable Dashboard P&L widget, and the observability/strategy-intelligence
wave: pipeline-latency metrics + Dashboard tile, daily health report (15:45
IST via notifications), performance-weighted sizer, sentiment decay curve,
Signal Outcomes viewer (Phase 4 complete), and circuit-breaker history.
Also 2026-07-10: the ML DATASET wave — DatasetBuilder service (1-minute-candle
reaction features + horizon targets per signal, look-ahead-safe feature/target
split), /api/dataset (column catalog, filtered rows, stats, CSV/JSONL export,
backfill) and the Dataset page (column picker with FEATURE/TARGET badges,
presets, filters, live preview, export). Wave 2 same day: SHADOW ROWS (every
analyzed-but-never-signaled announcement is enriched too — the negatives a
materiality pre-filter model needs; typically a 10-100x row multiplier),
context features (IST session clock, concurrent NIFTY move, excess return),
cross-listing dedup (NSE+BSE twins share a key), chronological train/val
split export, and the column-health / leakage report (/api/dataset/health).
Wave 3: FULL-HISTORY backfill — POST /api/dataset/backfill?full=true loops
batches in the background until every pending row is enriched (live progress
at GET /api/dataset/backfill/status; "Fill entire history" button + progress
line on the Dataset page). Efficiency: ONE Fyers history fetch per symbol
per batch (the range call covers all of a symbol's pending rows),
DATASET_FETCH_DELAY_SECONDS paces cache-miss fetches, and filings outside
the IST session are settled terminally as "after_hours" with zero API calls
(the majority of the shadow universe). Day-high targets (day_high,
ret_day_high_pct — the peak of the rest of the trading day) joined the
horizon targets. As-of pricing: illiquid names that don't trade in the
exact signal minute take the last trade at-or-before each minute (bounded
by the session close), rescuing rows that were wrongly no_candles. The
builder runs its DB work on a PRIVATE thread pool so a full backfill never
starves the HTTP surface.
Wave 4: the FULL minute-by-minute trajectory — price_1m..price_15m and
ret_1m..ret_15m for EVERY minute (not just 1-5 + 15) so a model learns the
SHAPE of the move; minutes 1-5 are decision-time features, 6-14 are
look-ahead targets ("trajectory" column category). Feature-schema is
versioned (features_version, FEATURES_VERSION const): POST
/api/dataset/backfill?rebuild=true re-enriches already-complete rows whose
schema predates the current version ("Rebuild schema" button), so a new
feature set rolls over the whole history without a wipe.
Also 2026-07-10: the TRADE PAGE wave — full TradingView-style interactive
charting (lightweight-charts v5, 7 timeframes, 4 chart types, 9 overlays
incl. SMA/EMA/VWAP/Bollinger/Supertrend/Ichimoku, 9 oscillators incl. RSI/
MACD/Stochastic/ADX/ATR, drawing tools with localStorage persistence, price
alerts, compare symbols, infinite scroll-back), manual order placement (real-
money only, risk-gated with operator bypass), option chain panel (live Fyers
with instrument-master fallback), symbol search + autocomplete (Fyers scrip
master CSV loader), positions panel with live P&L, and pending-orders
management. Fyers setup automation (one-click OAuth from the Accounts page,
pre-validation of app_id, authorize-url CSRF, tunnel + webhook helpers).
Accounts page overhaul (Paper + Fyers cards, FyersCredentialsCard,
FyersSetupCard, FyersWebhookCard).
Backend suite: 61 pytest modules, ~900 tests.

--------------------------------------------------------------------------------
                              TABLE OF CONTENTS
--------------------------------------------------------------------------------
  1.  What It Does
  2.  Why It Exists (The Edge)
  3.  Architecture Pipeline & Detailed Workflow
  4.  Core Tracks (T1 - T6) and Module Map
  5.  Technology Stack
  6.  Directory Layout
  7.  Key Concepts & Trading Parameters
  8.  The Two-Track Analyzer (Fast Track / Hybrid / LLM)
  9.  The Rules Engine (operator logic)
 10.  The Risk Engine (hard gates R0–R13)
 11.  Circuit Breakers & Kill Switch
 12.  Execution, Trade Management & Market Data
 13.  Monitors (NSE / BSE scrapers)
 14.  Notifications & Webhooks
 15.  Backtesting
 16.  The REST API (router map)
 17.  Data Model (SQLAlchemy tables)
 18.  Configuration & Settings
 19.  The Frontend (React dashboard)
 20.  Improvement Roadmap (plan.txt)
 21.  Setup & Execution Guide
 22.  Scripts
 23.  Testing
 24.  Troubleshooting
 25.  Design Philosophy & Invariants

--------------------------------------------------------------------------------
1. WHAT IT DOES
--------------------------------------------------------------------------------
The AI News Trading Bot is an event-driven, low-latency intraday trading system.
It continuously polls NSE and BSE corporate-announcement feeds. When a fresh
material filing appears (order win, results, resignation, buyback, M&A, …) it:

  1. Detects and de-duplicates the filing (SHA-256 content hash).
  2. Optionally pre-fetches and extracts the filing PDF text (PyMuPDF).
  3. Classifies the event — either deterministically (fast track) or via the
     DeepSeek LLM — into a structured JSON verdict (event type, sentiment,
     sentiment score, confidence, recommendation, key numbers).
  4. Evaluates the verdict against operator-defined rules (the rules engine),
     producing a BUY / SELL / HOLD / BLOCK action.
  5. Sizes and gates the trade through a strict, non-bypassable risk engine and
     portfolio circuit breakers.
  6. Executes it through an explicit ENTRY STATE MACHINE — anti-chase drift
     gate (with a passive retracement watch), per-symbol mutex, IOC
     marketable-limit routing, dual-confirmation fill watch (order-WS + REST),
     partial-fill handling, and flash-crash / slippage-breach emergency
     flattens — simulated (paper backend) or real (Fyers).
  7. Manages the position in REALTIME (every Fyers WebSocket tick, not a poll):
     stop-loss, target, breakeven lock, trailing stop, scale-out,
     consolidation/stall exits, time exit, EOD square-off. Live exits route
     through the broker (marketable limit → market fallback, retries,
     critical escalation); a 60s reconciliation loop syncs externally-closed
     positions (CLOSED_EXTERNAL).
  8. Streams every step to the React dashboard over WebSockets and fans events
     out to notification channels and webhooks.

Everything is controlled from the browser UI. There are no file sentinels and no
CLI-only gates — every operational switch lives in the dashboard.

--------------------------------------------------------------------------------
2. WHY IT EXISTS (THE EDGE)
--------------------------------------------------------------------------------
Material corporate announcements move NSE/BSE stocks within seconds. Retail
traders reacting to Telegram/Twitter alerts enter the move halfway through. This
bot automates the "first read" and "first trade" to capture the initial spike,
while enforcing strict pre-trade risk controls so bad data or an LLM
hallucination cannot blow up the account.

Core belief (see memory: "Early entry is the edge"): enter fast on the news
spike; never fix whipsaw or slippage by delaying entry — fix it in the stop,
the sizing, and the slippage caps. News alpha decays in minutes, which is why
the whole system is intraday-only and time-boxed.

--------------------------------------------------------------------------------
3. ARCHITECTURE PIPELINE & DETAILED WORKFLOW
--------------------------------------------------------------------------------
One FastAPI process. All internal messaging goes over an in-memory async event
bus (app/services/event_bus.py) — publish/subscribe channels, no external
broker. Six logical tracks (T1–T6) run as asyncio tasks under the app lifespan.

   [NSE / BSE filings]
          │  poll every POLL_INTERVAL_SECONDS
          ▼
   T2 Monitors ──publish──► "announcements.new"
          │  (dedupe by content_hash, prefetch PDF, drop Hindi/junk)
          ▼
   T3 Analyzer  (subscribes announcements.new)
          │  staleness gate → AI on/off → pre-LLM noise filter →
          │  FAST TRACK (headline) | HYBRID (headline+PDF) | LLM (DeepSeek)
          │  → schema validate → rules engine → pipeline deadline
          ▼
      "signals.new"  (BUY / SELL / HOLD / BLOCK, entry/SL/target in rationale)
          │
          ▼
   T4 Execution (subscribes signals.new; one asyncio task per signal)
          │  circuit-breaker entry check → RiskEngine.evaluate (hard gates +
          │  sizing) → EntryManager state machine:
          │    INITIALIZED → AWAITING_QUOTE → DRIFT_CHECK → ORDER_ROUTING →
          │    FILL_MONITORING → RECONCILING → PARTIAL_HANDLING →
          │    ACTIVE_POSITION   (off-ramps: RETRACEMENT_WATCH, EXPIRED,
          │    REJECTED, SYMBOL_LOCKED, BROKER_TIMEOUT, SLIPPAGE_BREACH,
          │    FLASH_CRASH_EXIT)
          ▼
   [PaperBackend]  or  [FyersLiveBackend]  ──► "trade.executed" (exact fill qty)
          │
          ▼
   TradeManager — realtime tick listeners on the MarketDataBus (Fyers WS) +
          │  0.5s backstop sweep → STOP / TARGET / BREAKEVEN / TRAIL /
          │  SCALE_OUT / CONSOLIDATION / STALL / TIME_EXIT / EOD square-off.
          │  LIVE exits route through the broker (marketable limit → market
          │  chase, retries, CRITICAL escalation); 60s broker reconciliation
          │  (CLOSED_EXTERNAL) → realised P&L → "trade.closed"
          ▼
   T5 Notifications + Webhooks   |   T6 Operational REST/WS surfaces (UI)
   Phase-4 OutcomeLogger records price at signal, +5m, +30m for every signal.

--- DETAILED STEP-BY-STEP (analyzer, app/analyzer/service.py) ---

 0. Idempotency: skip if an `analyses` row already exists for the announcement.
 0.5 AI master switch: if AI_ANALYSIS_ENABLED is OFF (Dashboard toggle), store a
     placeholder analysis (`ai_analysis_disabled`) and stop — no LLM, no signal.
 1. Staleness gate: if (now − filed_at) > MAX_NEWS_AGE_SECONDS, store a
     `stale_news` placeholder and stop. A startup sweep also pre-marks the whole
     backlog so a restart/replay never fires the LLM on old news.
 2. Pre-LLM noise filter (PRE_LLM_FILTER_ENABLED): drop clearly-administrative
     filings (trading-window notices, compliance certs, newspaper publications)
     before paying for an LLM call; keeps the serial queue clear for real movers.
 3. Fast track (FAST_TRACK_ENABLED): a few unambiguous headline shapes emit a
     signal in milliseconds with NO LLM. Non-matches fall through.
 4. Extracted-text / hybrid: optionally download + extract the PDF (cache-first;
     the monitor prefetched it). Hybrid parses an order value from the text.
 5. LLM track: render system+user prompts, call DeepSeek with a hard timeout,
     validate the JSON against the Pydantic schema.
 6. Rules engine: evaluate the analysis against every ENABLED strategy's enabled
     rules (priority ASC, first match wins) → action + params.
 7. Trade levels: compute entry / stop-loss / target / RR (v1 deterministic
     stub packed into the signal rationale string).
 8. Pipeline deadline (PIPELINE_DEADLINE_SECONDS, 0 = off): if the fully-analysed
     filing is now older than the deadline, store the analysis (training data)
     but BLOCK the signal (`pipeline_deadline`) — a late entry is worse than none.
 9. Persist analysis + signal; publish "signals.new"; write an audit entry.

Concurrency: up to 2 announcements are analysed in parallel (bounded semaphore
to respect DeepSeek rate limits). Prompt templates, rules, and the sector map
are cached in-process for 5 minutes and invalidated on `settings.updated`.

--------------------------------------------------------------------------------
4. CORE TRACKS (T1 - T6) AND MODULE MAP
--------------------------------------------------------------------------------
T1 — Infrastructure
  app/main.py            FastAPI app, lifespan wiring (starts every service),
                         correlation-id + access-log middleware, static SPA host.
  app/config.py          Pydantic Settings (all env/UI-tunable parameters).
  app/logging_config.py  structlog JSON logging with per-request correlation ids.
  app/db/                session.py (engine/Base), models.py (business tables),
                         infra_models.py (app_settings k/v), init.py (create_all).
  app/services/          event_bus.py (async pub/sub), instrument_master.py
                         (Fyers scrip-master CSV loader — NSE/BSE cash + F&O;
                         in-memory search index with exact/prefix/fuzzy
                         ranking, option-chain builder per underlying,
                         downloadable via POST /api/search/refresh; falls
                         back to a small in-process seed on fresh install),
                         audit_service.py, outcome_logger.py, webhook_service.py,
                         health_report.py (daily 15:45-IST operator summary →
                         system.report → notification channels),
                         dataset_builder.py (periodic 1-min-candle enrichment
                         of signal_outcomes AND analyzed-but-never-signaled
                         announcements (shadow rows) into dataset_features —
                         reaction features, horizon targets and session/NIFTY
                         context for ML training).

T2 — Monitors (app/monitors/)
  base.py                BaseMonitor loop; reads live POLL_INTERVAL_SECONDS each
                         tick; dedup; publishes "announcements.new".
  nse.py / bse.py        Exchange-specific fetchers + payload parsers.
  manager.py             Owns the NSE+BSE tasks (start/stop, idempotent).
  browser_pool.py        Warm shared Playwright browser (fallback scraping path).

T3 — Analyzer (app/analyzer/)
  service.py             The pipeline heart (see §3).
  fast_track.py          Deterministic headline→signal (+ hybrid PDF value parse).
  deepseek_client.py     DeepSeek HTTP client (retries, timeouts, token/cost).
  prompts.py             Prompt rendering, event-type detection, non-material
                         denylist, DEFAULT template seeding.
  schemas.py             Pydantic AnalysisResponse (15 EventType values + DEFAULT).
  rules_engine.py        Pure rule evaluation (see §9).
  pdf_cache.py           Background PDF prefetch + cache.
  pdf_extract.py         PyMuPDF text extraction, Hindi filter, page scoring.
  default_rules.py       Seeds the default paper broker account (NOT rules).

T4 — Execution (app/execution/)
  manager.py             Subscribes signals.new (one asyncio task per signal);
                         risk-gates; delegates order placement to the
                         EntryManager and owns persistence/eventing for every
                         outcome; caches one backend per broker account;
                         hot-reloads on mode change; attaches volatility/funds/
                         quote providers; resolves live exit backends for the
                         TradeManager (exit_backend_for_account).
  entry_manager.py       The ENTRY STATE MACHINE (one attempt per signal):
                         per-symbol mutex (SYMBOL_LOCKED), anti-chase drift
                         gate vs the analysis-time price, IOC marketable-limit
                         routing tagged with a client_order_id, dual-
                         confirmation fill watch (order-WS event fast path +
                         REST /orders poll), partial-fill handling (exact qty
                         handover, remainder cancelled), zero-fill EXPIRED (no
                         retry), BROKER_TIMEOUT (never retried blind), and the
                         post-fill safety checks: SLIPPAGE_BREACH (fill through
                         the limit → immediate market flatten) and
                         FLASH_CRASH_EXIT (market already through the stop at
                         fill time → immediate market flatten). Also hosts the
                         RetracementMonitor: drift-blocked signals wait
                         passively (no lock) for a pullback into the band
                         within RETRACEMENT_WINDOW_SECONDS, re-arm once with
                         the ORIGINAL anchor + deadline, else expire.
  base.py                TradingBackend interface + OrderResult types.
  paper.py               PaperBackend — synthetic fills with slippage/latency.
  fyers_live.py          FyersLiveBackend — real REST order placement (httpx).
  fyers_auth.py / fyers_login.py  OAuth token flow.
  fyers_stream.py        Realtime Fyers WebSockets (data socket = prices,
                         order socket = fills) feeding the market-data bus.
  market_data.py         In-process MarketDataBus (last quotes + quote sink +
                         per-symbol subscription queues for tick listeners).
  quote_feed.py          Refreshes watched symbols (WS-first, REST fallback).
  trade_manager.py       Post-entry lifecycle, REALTIME: a tick listener per
                         managed symbol reacts to every MarketDataBus tick
                         (Fyers WS) — STOP/TARGET/BREAKEVEN/TRAIL/SCALE_OUT/
                         CONSOLIDATION/STALL/TIME_EXIT + EOD square-off; the
                         0.5s sweep is only a backstop. LIVE exits route
                         through the broker (marketable limit → market chase,
                         rejection retries, system.error escalation); paper
                         settles directly. Stale-tick guard (no price exit on
                         a frozen feed), 60s broker reconciliation
                         (CLOSED_EXTERNAL / external resize), restart
                         hydration, realised P&L, manual close/close-all.
  order_reconcile.py     Reconciles broker order/fill state (WS + postback +
                         REST dedup by broker_order_id; a filled row is never
                         downgraded by a late cancel echo).
  symbols.py             Resolve bare symbol → full Fyers symbol; scrip master.

T4-risk — Risk (app/risk/)
  engine.py              RiskEngine.evaluate — hard gates R0–R13 + position size.
  position_sizer.py      Equity ledger, risk-% ramp, notional caps, Fyers funds.
  perf_sizer.py          Performance-weighted sizer: per-event-type win rate +
                         avg R from closed trades tier the per-trade risk %.
  circuit_breakers.py    Portfolio breakers + kill switch (RiskState singleton).
  volatility.py          ATR provider (Fyers candles) + India-VIX regime (fail-safe).
  event_profiles.py      Per-event risk profile matrix.
  market_clock.py        IST session / entry-window / square-off logic.

T5 — Notifications & Webhooks
  app/notifications/     manager.py + telegram.py, discord.py, email.py, webhook.py.
  app/webhooks/          dispatcher.py (outbound), receiver.py + fyers.py (inbound),
                         signing.py (HMAC-SHA256 sign/verify).

T6 — Operational surfaces (app/api/) — REST + WS control plane consumed by the UI.
  25 router modules: core.py, settings_api.py, trading_mode.py, prompts.py,
  rules.py, strategies.py, broker_accounts.py, fyers_callback.py (OAuth +
  status + disconnect), fyers_postback.py, market.py (indices + chart history),
  search.py (symbol autocomplete + option-chain), options.py (live Fyers chain),
  orders.py (manual order placement + cancel + pending + quote), positions.py
  (managed book + close + close-all + update levels), risk.py, outcomes.py,
  dataset.py, metrics.py, backtest.py, notifications.py, webhooks.py,
  audit_log.py, health.py, ws.py.

--------------------------------------------------------------------------------
5. TECHNOLOGY STACK
--------------------------------------------------------------------------------
Backend (requirements.txt, Python 3.11)
  fastapi==0.115.6            uvicorn[standard]==0.32.1     websockets==13.1
  python-multipart==0.0.19    pydantic==2.10.3              pydantic-settings==2.7.0
  SQLAlchemy==2.0.36          alembic==1.14.0               structlog==24.4.0
  httpx==0.28.1               fyers-apiv3==3.1.6 (realtime WS feeds only; REST
                              orders/quotes use httpx)      playwright==1.49.1 (lazy)
  PyMuPDF==1.25.1 (lazy; extracted-text mode)               pytest==8.3.4
  pytest-asyncio==0.24.0      orjson (optional fast JSON parse if installed)

Frontend (frontend/package.json — React 18 + TS 5.7 + Vite 6)
  react 18.3 / react-dom 18.3        @tanstack/react-query 5.62 (server state)
  recharts 2.13 (P&L / equity charts) lightweight-charts 5.2 (candles)
  typescript 5.7                      vite 6                vitest 2 (+ testing-library)
  NOTE: no @dnd-kit — rule/prompt ordering uses a plain reorder endpoint,
        not drag-and-drop libraries.

Storage
  SQLite at data/trading.db (WAL/SHM for concurrency). In pytest, TESTING=1
  swaps to sqlite:///:memory:. NSE/BSE scrip-master cached locally under data/.

Pricing: FYERS-ONLY. All quotes/candles/VIX come from Fyers. There is NO Yahoo
or public-feed fallback — it was removed on purpose (memory: "Fyers-only
pricing"). When the live feed is cold, the system blocks rather than filling at
a synthetic price (memory: "No synthetic entry fill").

--------------------------------------------------------------------------------
6. DIRECTORY LAYOUT
--------------------------------------------------------------------------------
AI News Trading Bot/
├── app/                       # Backend package (FastAPI)
│   ├── main.py  config.py  logging_config.py  __init__.py (__version__)
│   ├── analyzer/              # T3: fast-track + DeepSeek + rules engine
│   ├── api/                   # T6: REST + WS routers (see §16)
│   ├── backtest/             # engine.py + metrics.py (sandboxed replay)
│   ├── db/                    # models.py, infra_models.py, session.py, init.py
│   ├── execution/             # T4: paper/live backends, trade + quote managers
│   ├── monitors/              # T2: NSE/BSE scrapers + browser pool
│   ├── notifications/         # T5: telegram/discord/email/webhook channels
│   ├── risk/                  # engine, sizer, circuit breakers, volatility, clock
│   ├── services/              # event bus, instrument master, audit, outcomes
│   ├── webhooks/              # inbound/outbound webhook dispatch + HMAC signing
│   └── tests/                 # 61 pytest modules (~900 tests) + conftest
├── frontend/                  # React + Vite dashboard (see §19)
│   ├── src/pages/             # 13 route views
│   ├── src/components/        # dashboard/, trade/, accounts/, rules/, prompts/,
│   │                          # settings/, common/, positions/, strategies/,
│   │                          # backtest/, notifications/, webhooks/
│   ├── src/hooks/  src/api/  src/lib/ (indicators.ts, roundtrips.ts)
│   ├── themes.ts  router.tsx  App.tsx  types.ts
│   └── src/tests/             # 7 vitest suites
├── scripts/                   # run/dev (ps1+sh), Fyers setup, seeders, smoke tests
├── data/                      # SQLite DB + scrip-master caches (gitignored)
├── logs/                      # app / websocket / cloudflared logs (gitignored)
├── plan.txt                   # consolidated improvement roadmap (Phases 0–6)
├── PROJECT.txt                # this file
├── requirements.txt  pyrightconfig.json  .env.example  .gitignore
└── .claude/launch.json        # preview dev-server config (name: "frontend")

--------------------------------------------------------------------------------
7. KEY CONCEPTS & TRADING PARAMETERS
--------------------------------------------------------------------------------
• Trading mode (TRADING_MODE): "paper" (simulated) or "live" (real Fyers).
  Switched via POST /api/settings/trading-mode with a literal `confirm: true`
  body; the UI adds a typed-"LIVE" confirmation. The old `.i_accept_live_risk`
  file sentinel has been REMOVED — control is frontend-only. On switch, the
  Settings cache hot-reloads and `settings.updated` fires so the execution
  manager drops cached live backends. Turning Fyers OFF blocks trades but keeps
  the price feed + OAuth alive (memory: "Fyers enabled = trade switch only").

• Intraday only, no carry-forward: every trade is MIS/intraday. TIME_EXIT and
  EOD square-off always flatten. There is deliberately no delivery/overnight
  path (memory: "Intraday only, no carry-forward").

• The speed budget: news alpha is gone in seconds/minutes.
    MAX_NEWS_AGE_SECONDS   — reject news older than this before the LLM.
    LLM_TIMEOUT_SECONDS    — discard the opportunity if DeepSeek is slow.
    PIPELINE_DEADLINE_SECONDS — hard end-to-end deadline (0 = off) → BLOCK late.
    MAX_HOLD_SECONDS       — force a TIME_EXIT after the spike-capture window.

• Two-track analyzer: deterministic fast track for a few unambiguous headline
  shapes; DeepSeek for everything else. Both feed the same rules + risk path.

• Operator-curated rules: there are NO hard-coded trading triggers. Startup no
  longer seeds default rules; the operator builds them in the Rules UI and a
  restart never resurrects deleted rules (memory: "Rules: fresh slate").

• Intraday leverage (INTRADAY_LEVERAGE, ~5x): NOTIONAL caps (sizing clamp,
  single-name cap, sector cap) are computed against buying power = equity ×
  leverage. Real-money limits (per-trade risk %, daily/weekly/monthly loss)
  stay on raw equity — leverage multiplies exposure, never the money at risk.

• Entry state machine + anti-chase: every approved signal runs an explicit
  lifecycle (drift check → IOC routing → dual-confirmation fill → handover).
  A price that ran >ENTRY_MAX_DRIFT_PCT past the analysis price is NOT chased:
  the signal parks in a passive retracement watch and only trades if the price
  pulls back into the band within the window — else it expires untraded.

• Realtime exit management: stop/target/breakeven/trailing evaluate on every
  Fyers WebSocket tick (per-symbol tick listeners on the MarketDataBus), not a
  polling interval. Live exits execute AT the broker (marketable limit →
  market fallback) for both protective and profit-taking paths; paper settles
  directly. Identical rule logic in both modes.

• Fail-safe defaults: missing market data (VIX, spread, ATR, ADV) makes the
  relevant filter SKIP (warn) or, in live mode for liquidity, BLOCK — it never
  fakes a pass. A cold live price feed blocks the trade (NO_LIVE_PRICE); a
  stale live tick never fires a price-based exit.

--------------------------------------------------------------------------------
8. THE TWO-TRACK ANALYZER (FAST TRACK / HYBRID / LLM)
--------------------------------------------------------------------------------
Fast track (app/analyzer/fast_track.py, FAST_TRACK_ENABLED, default per config
ON — see §11 drift note). Tight, high-conviction patterns only:
  1. ORDER_WIN with an explicit INR value ("bags order worth Rs 450 crore").
     Confidence scales with value. INR only (Rs/₹/INR + crore/cr/lakh/lac/
     million/mn/billion/bn), converted to crore.
  2. KMP resignation — MD / CEO / CFO / whole-time director / chairman. Routine
     succession ("resignation and appointment") and independent-director exits
     are left to the LLM.
  3. BUYBACK with an explicit INR value.
Hybrid path: an order-context headline with NO value (typical NSE phrasing) →
  extract the PDF text and parse the value deterministically near the order
  mention (~1s, still no LLM); cancellation wording anywhere in the text kills
  it. Known accepted blind spot: deal value is NOT scaled vs market cap — gate
  with rules (deal_value_inr_crore thresholds) until a market-cap feed lands.

LLM track (DeepSeek). Prompt templates live in the `prompt_templates` table and
are fully operator-editable (Prompts page) with version history. A single
DEFAULT template handles all event types; the detected event type is injected as
context. Templates carry model, temperature, max_tokens, and DeepSeek-v4
inference controls: reasoning_effort (low|medium|high), thinking_enabled,
stream. The completion is capped by min(template.max_tokens, LLM_MAX_TOKENS).

Extracted-text mode (SEND_EXTRACTED_TEXT, default OFF): when ON, DeepSeek gets
the real filing text (Hindi lines dropped, first 2 pages + keyword/digit-scored
pages, ~24k-char budget) plus a NO-TRADE-by-default footer requiring a BUY/SELL
to cite a fact from the filing. Any download/extraction failure (scanned PDF,
encryption, missing wheel, timeout) falls back to the legacy metadata-only
prompt — this mode can degrade but never block a signal (memory:
"Extracted-text toggle, non-destructive").

AnalysisResponse schema (schemas.py):
  event_type ∈ {ORDER_WIN, ACQUISITION, MERGER, DIVIDEND, BUYBACK, STOCK_SPLIT,
  BONUS, RIGHTS_ISSUE, Q1_RESULTS, Q2_RESULTS, Q3_RESULTS, Q4_RESULTS,
  ANNUAL_RESULTS, BOARD_MEETING, OTHER} (+DEFAULT for templates);
  summary; sentiment ∈ {positive, neutral, negative}; sentiment_score −100..100
  (fractional 0..1 scores auto-scaled ×100); confidence 0..1;
  recommendation ∈ {BUY, SELL, HOLD}; reasoning; key_numbers{deal_value_inr_crore,
  stake_change_pct, dividend_per_share, buyback_value_inr_crore}. Invalid/novel
  categories are coerced to OTHER rather than discarding the analysis.

--------------------------------------------------------------------------------
9. THE RULES ENGINE (app/analyzer/rules_engine.py)
--------------------------------------------------------------------------------
Pure function over loaded rules and the analysis dict. Live evaluation runs every
ENABLED strategy's enabled rules together, ordered by (priority ASC, id ASC),
first match wins; no match → HOLD fallback (persisted for observability but
marked blocked so nothing trades).

Conditions JSON supports `all_of` (AND) and `any_of` (OR) groups of
{field, op, value}. A rule matches when all_of is fully satisfied AND any_of has
≥1 satisfied entry (missing/empty group = satisfied).

Operators: ==, !=, in, not_in, >=, <=, >, <, between ([low, high] inclusive).
  == / != are case-insensitive for strings; numeric ops on a missing value are
  a non-match (fail-safe); malformed rules are skipped with a log warning.

Fields:
  Analysis:  event_type, sentiment, sentiment_score, confidence, recommendation,
             deal_value_inr_crore, stake_change_pct, dividend_per_share,
             buyback_value_inr_crore.
  Market:    sector (enriched live from the sector map), price, change_pct,
             adv_crore (enriched only when a live quote is supplied),
             atr_pct, india_vix, spread_pct (schema-ready; populate in Phase 5).

The UI rule builder is limited to 4 typed conditions (memory: "Rules: fresh
slate & cross-strategy eval"); the engine itself is more general. Rules can be
dry-run against a sample analysis via GET/POST /api/rules/dry-run.

--------------------------------------------------------------------------------
10. THE RISK ENGINE (app/risk/engine.py)
--------------------------------------------------------------------------------
RiskEngine.evaluate(signal, …) → RiskDecision{approved, violations[], sizing,
context}. Every signal routes through it; there is no override flag, no
force=True, no "if is_test" branch — the only way a rule fails to apply is by
bypassing the manager, and the manager routes every signal through the engine.

Position sizing:
    qty = floor( (equity × risk_pct/100) / |entry − stop_loss| )
  then clamped to the notional single-name cap (the SMALLER of risk-based and
  notional-based qty wins). risk_pct = strategy/settings ceiling, first TIERED
  by the performance-weighted sizer (app/risk/perf_sizer.py: the event type's
  realised win rate + avg R over the last N closed trades promote to
  PERF_SIZER_HIGH_RISK_PCT or demote to LOW; fewer than MIN_TRADES closed
  trades → base unchanged; manual orders never tiered), then reduced by the
  graduated new-account ramp and any RiskState throttle and a (fail-safe) VIX
  multiplier. Notional caps use buying power = equity × INTRADAY_LEVERAGE; equity
  is a real ledger (capital + realised + unrealised), anchored in LIVE mode to
  the real Fyers balance when a funds feed is attached (else PORTFOLIO_VALUE).

Hard gates (each blocks the signal):
  R1  action must be BUY/SELL (HOLD/BLOCK → blocked)
  R2  confidence > 0
  R2b confidence ≥ MIN_SENTIMENT_CONFIDENCE floor (manual orders carry 1.0)
  R0  stop_loss present and > 0 (no SL → cannot size safely → block)
  R0b direction-aware stop sanity: stop == entry → RISK_INVALID_STOP_DISTANCE
      (zero risk distance cannot be sized); BUY stop above entry →
      RISK_STOP_ABOVE_ENTRY; SELL stop below entry → RISK_STOP_BELOW_ENTRY
      (a wrong-side stop would trigger instantly / never protect)
  R10 symbol not on the strategy's blocklist
  R11 shorting: a SELL that OPENS a short requires SHORTING_ENABLED (closing a
      long is always allowed)
  RISK_NO_ENTRY_PRICE  entry from quote if not supplied; none → block
  R3  qty ≥ 1 (rounds to 0 → block)
  R4  trade risk ≤ MAX_CAPITAL_RISK_PCT of equity
  R5  daily realised loss < DAILY_MAX_LOSS_PCT of equity
  R6  open positions < MAX_CONCURRENT_POSITIONS (adding to a held name is exempt)
  R7  (existing + new) single-name value ≤ MAX_SINGLE_POSITION_PCT of buying power
  R8  min liquidity: block if known ADV < MIN_LIQUIDITY_CRORE; unknown ADV →
      block in LIVE only when REQUIRE_KNOWN_LIQUIDITY, else warn+allow
  R12 bid/ask spread ≤ MAX_SPREAD_PCT (fail-safe skip until an L1 feed exists)
  R9  sector exposure ≤ SECTOR_CONCENTRATION_PCT of buying power
  R13 distinct names in a sector ≤ MAX_POSITIONS_PER_SECTOR, AND the
      one-name-per-event window: block a 2nd same-sector entry within
      SECTOR_CLUSTER_WINDOW_SECONDS of the first (correlated news cluster)

Volatility-aware stops (ATR_ENABLED): when ATR is available the initial stop is
ATR×ATR_STOP_MULT clamped to [DEFAULT_SL_MIN_PCT, ATR_MAX_STOP_PCT] of entry;
otherwise the percentage stop applies. ATR comes from Fyers history candles and
is fail-safe (no account → % stop). (memory: "Risk edge enhancements").

Static sector map: ~60 large-caps across IT, BANK, NBFC, ENERGY, FMCG, AUTO,
PHARMA, METALS, INFRA, TELECOM, CEMENT, POWER, REALTY, CONSUMER_DURABLES.
Unknown symbol → sector rules simply don't apply (allow).

--------------------------------------------------------------------------------
11. CIRCUIT BREAKERS & KILL SWITCH (app/risk/circuit_breakers.py)
--------------------------------------------------------------------------------
A persistent, account-wide safety layer ABOVE the per-trade engine, backed by the
singleton `risk_state` row (survives restarts). A background monitor in the
lifespan calls evaluate_breakers() every ~10s; entry_allowed() is checked before
every new entry.

Breakers (thresholds from Settings; anchors reset on the IST calendar):
  daily loss ≥ DAILY_MAX_LOSS_PCT              → halt + flatten (clears next day)
  monthly drawdown ≥ MONTHLY_MAX_DRAWDOWN_PCT  → halt + flatten + force paper
                                                 (clears next month)
  weekly loss ≥ WEEKLY_MAX_LOSS_PCT            → throttle risk % (WEEKLY_BREACH_RISK_PCT)
  consecutive losers ≥ MAX_CONSECUTIVE_LOSERS  → cooldown pause
                                                 (CONSECUTIVE_LOSER_PAUSE_MINUTES;
                                                  a high-conviction signal ≥
                                                  CONSECUTIVE_LOSER_RESUME_CONFIDENCE
                                                  can resume early)
  trades today ≥ MAX_TRADES_PER_DAY            → no new entries
A manual kill (POST /api/risk/kill) only clears via POST /api/risk/resume.
On a flatten, TradeManager.close_all(reason="CIRCUIT_BREAKER") squares off.

--------------------------------------------------------------------------------
12. EXECUTION, TRADE MANAGEMENT & MARKET DATA
--------------------------------------------------------------------------------
Execution manager (app/execution/manager.py): subscribes signals.new and spawns
ONE asyncio task per signal (concurrent attempts are what the symbol mutex
serialises); loads the signal + its strategy + the strategy's broker account
(the routing key); picks a backend (paper when account.paper_mode OR
TRADING_MODE=='paper', else Fyers live); parses entry/SL/target from the signal
rationale (falls back to the last cached quote); runs the pre-entry gates + the
risk engine; on block writes a risk_events row, marks the signal blocked,
persists a blocked trade, publishes risk.blocked (no order); on approve hands
the order to the EntryManager state machine and persists/publishes its outcome.
One backend is cached per broker account; a Fyers OAuth token rotation
re-injects the new token; `settings.updated` drops mode-coupled backends.

THE ENTRY STATE MACHINE (app/execution/entry_manager.py) — every approved
signal runs one attempt through explicit states:
  INITIALIZED → AWAITING_QUOTE (freshest bid/ask off the bus) → DRIFT_CHECK →
  ORDER_ROUTING → FILL_MONITORING → RECONCILING → PARTIAL_HANDLING →
  ACTIVE_POSITION; off-ramps: RETRACEMENT_WATCH, EXPIRED, REJECTED.
Safety features:
  • Symbol mutex: a second signal for a symbol already routing waits
    SYMBOL_LOCK_WAIT_SECONDS then rejects (SYMBOL_LOCKED) — no double entry
    when NSE + BSE publish the same news. The retracement watch holds NO lock,
    so a genuinely fresh signal for the symbol still trades.
  • Anti-chase drift gate: adverse move beyond ENTRY_MAX_DRIFT_PCT vs the
    ANALYSIS-TIME price blocks the entry (favourable drift always passes and
    the limit anchors on the live price). Blocked signals park in the
    RetracementMonitor: pull back inside the band within
    RETRACEMENT_WINDOW_SECONDS → re-armed ONCE with the original anchor +
    deadline (staleness gate skipped — the window is its own freshness bound);
    no pullback → signal expired, never traded. Against a simulated paper
    quote the drift gate is a no-op (sim prices live in another universe).
  • IOC marketable-limit routing (live price ± ENTRY_BUFFER_PCT), tagged with
    a client_order_id for cross-path dedup.
  • Dual-confirmation fill watch: the order-WS `trades.filled` event is the
    fast path; REST /orders is polled at the ORDER_FILL_TIMEOUT_SECONDS mark
    and finally at ENTRY_FILL_TIMEOUT_SECONDS. First confirmation wins; the DB
    layer (order_reconcile) dedupes the duplicates by broker_order_id.
  • Partial fill: the position hands over with the EXACT filled quantity and
    the ORIGINAL stop (rupee risk scales down with size); the remainder is
    cancelled. Zero fill: cancelled + EXPIRED, never retried.
  • BROKER_TIMEOUT: a place_order that hangs past BROKER_ORDER_TIMEOUT_SECONDS
    ends the attempt with NO retry (a blind retry is how double orders
    happen).
  • Post-fill safety: a fill THROUGH the limit (exchange glitch) → immediate
    market flatten (SLIPPAGE_BREACH, critical); market already through the
    stop at fill time → immediate market flatten (FLASH_CRASH_EXIT). Both
    persist the entry + exit trades and a critical risk event; nothing is
    handed to the TradeManager.

TRADE MANAGER (app/execution/trade_manager.py) — realtime, both modes:
Registers each filled entry as a ManagedPosition (invalid wrong-side targets
are clamped to 0.5R at registration). Every managed symbol gets a TICK
LISTENER on the MarketDataBus — the Fyers WS publishes straight into it — so
exit rules run on EVERY tick; the 0.5s sweep is a backstop (time exits,
mark-to-market, hydration, EOD square-off) and doubles as a synchronisation
barrier for in-flight exits. Per-symbol locks make tick + sweep evaluation
atomic (no double exits on a tick storm).

Exit rule order (deliberate): TIME_EXIT (allowed even on a stale tick, settles
at the last real price) → freshness gate → BREAKEVEN lock (at
+BREAKEVEN_AT_PCT the stop moves to entry ± BREAKEVEN_LOCK_PCT, tighten-only)
→ scale-out + trailing (open-ended positions only; an explicit target still
means full exit — memory: "Explicit levels win over scale-out"; scale-out is
tighten-only vs an armed breakeven stop) → hard STOP checked BEFORE target
(a gap through both fires the protective exit) → CONSOLIDATION exit (profit
1–2.5% pinned in ≤CONSOLIDATION_RANGE_PCT for the whole window → free the
capital) → STALL exit (profit 3–6% with rate-of-change < STALL_ROC_PCT over
the window → capture before reversion).

Exit EXECUTION routes by account: paper settles directly (the bot IS the paper
broker); LIVE exits go to the broker FIRST — a marketable-limit order (bid/ask
∓ EXIT_BUFFER_PCT, IOC), anything unfilled at EXIT_FILL_TIMEOUT_SECONDS is
cancelled and chased at MARKET (protective exits must complete; scale-outs are
NOT chased — the unfilled slice stays managed). Rejections retry at market
with backoff up to EXIT_RETRY_MAX, then escalate ONCE: critical RiskEvent +
`system.error` event (→ Telegram/email) — and the position keeps retrying on a
cooldown, never silently abandoned. A hung broker call gets exactly one market
retry, then the position stays open under management (flagged). The local
settle happens only after the broker confirms, at the broker's average fill.

Safety rails: price-triggered exits require a LIVE tick younger than
STALE_QUOTE_SECONDS — a frozen WebSocket can never fire an exit at a stale
price (the static stop waits for the feed). A 60-second reconciliation loop
compares the Fyers book with the managed book: broker-flat → CLOSED_EXTERNAL
(manual close in the Fyers app, margin call, missed callback) + operator
alert, settled locally with NO order placed; broker-reduced → the local
quantity resizes to the broker's truth. Managed levels persist on the
`positions` row so exits re-arm after a restart (hydration also re-arms the
tick listeners). Backs the dashboard's manual close / square-off-all. Entry
and exit share ONE price source; a cold live feed blocks rather than filling
at a synthetic price (memory: "No synthetic entry fill").

Market data (app/execution/): MarketDataBus holds last quotes and a quote sink
that bridges every tick onto the /ws "quotes" channel (sub-second UI pushes).
QuoteFeed is WS-first (Fyers data socket) with a one-shot REST fallback; the
FyersStreamManager keeps the index symbols always-subscribed for the status-bar
ticker. Market-data lookups key on the Fyers token, not the `enabled` flag, so
disabling trading keeps prices flowing.

--------------------------------------------------------------------------------
13. MONITORS (app/monitors/)
--------------------------------------------------------------------------------
NSEMonitor and BSEMonitor each run an independent poll loop at the live
POLL_INTERVAL_SECONDS (picked up per-tick, so a UI change takes effect on the
next poll). They fetch the exchange announcement feed via httpx, with a warm
shared Playwright browser (browser_pool.py) as a fallback when the CDN blocks the
fast path. Each new filing is de-duplicated by content_hash =
SHA-256(exchange|symbol|filed_at|pdf_url), its PDF is prefetched in the
background (when fast-track / extracted-text is on and the title isn't
administrative junk), and an "announcements.new" event is published. Polling too
aggressively risks NSE/BSE 403/429 rate-limits — raise the interval if the logs
show `monitor.retry` spam.

--------------------------------------------------------------------------------
14. NOTIFICATIONS & WEBHOOKS
--------------------------------------------------------------------------------
Notifications (app/notifications/): a manager subscribes to the event bus and
fans events out to operator-configured channels. Channel kinds: telegram,
discord, email, webhook. Each channel has a JSON config and an events filter
(csv, e.g. signal,trade,risk_halt,error). Every send is recorded in
`notification_log` (sent | failed | skipped). The TradeManager's critical
escalations publish on `system.error` (event type "error"): EXIT_FAILED (the
broker kept refusing an exit — position still open, manual intervention may be
needed) and CLOSED_EXTERNAL (the broker closed a position outside the bot).
The daily health report publishes on `system.report` (event type "report") at
HEALTH_REPORT_TIME_IST — one plain-text summary of the trading day (signals /
blocks / trades / P&L / breaker status / WS feed health / DB size); operators
opt a channel in by adding "report" to its events filter.

Webhooks (app/webhooks/): the outbound dispatcher POSTs bus events to registered
webhook URLs (HMAC-SHA256 signed with the webhook's secret), recording each
attempt in `webhook_deliveries`. Inbound webhooks are accepted at
POST /api/webhooks/in/{id} (bearer-verified). Fyers order postbacks arrive at
POST /api/fyers/postback (HMAC-verified with FYERS_POSTBACK_SECRET);
scripts/register_fyers_webhook.py registers the row and copies the secret. Local
inbound delivery typically needs a public tunnel (e.g. cloudflared).

--------------------------------------------------------------------------------
15. BACKTESTING (app/backtest/)
--------------------------------------------------------------------------------
Replays historical announcements through the SAME analyzer→rules→risk→paper
pipeline. Critically, each run builds a throwaway in-memory SQLite SANDBOX seeded
with copies of the strategy, its rules, and the broker account, so simulated
analyses/signals/trades/positions never pollute the live Dashboard, Trade
History, or the live risk engine's concurrency/sector counts. The live DB is
touched only to read source announcements + config and to write the final
`backtest_runs.results` blob, which the Backtest page renders. Prices come from a
pluggable PriceModel (synthetic-return default; a real Fyers historical loader
can be swapped in). Single-threaded, chronological; metrics via metrics.summarise
(equity curve, win rate, R-multiples, drawdown).

--------------------------------------------------------------------------------
16. THE REST API (ROUTER MAP)  — all under the one FastAPI app
--------------------------------------------------------------------------------
Health / meta
  GET  /health                         GET  /api/version      GET /api/server-info
Settings (global k/v overrides, persisted, survive restart)
  GET  /api/settings                   PUT  /api/settings
  GET  /api/settings/credentials       PUT  /api/settings/credentials
  POST /api/settings/trading-mode      GET  /api/settings/live-risk-ack (legacy no-op)
Prompts (LLM templates + version history)
  GET  /api/prompts                    GET  /api/prompts/{event_type}
  GET  /api/prompts/{event_type}/history        GET /api/prompts/{event_type}/default
  POST /api/prompts/{event_type}       POST /api/prompts/{event_type}/restore/{version}
  POST /api/prompts/{event_type}/preview
Rules
  GET/POST /api/rules                  GET/PUT/DELETE /api/rules/{id}
  GET/POST /api/rules/dry-run          POST /api/rules/reorder
Strategies
  GET/POST /api/strategies             GET/PUT/DELETE /api/strategies/{id}
Broker accounts
  GET/POST /api/broker-accounts        PUT/DELETE /api/broker-accounts/{id}
  POST /api/broker-accounts/{id}/test
Fyers auth / postback
  GET  /api/fyers/callback  /authorize-url  /status     POST /api/fyers/disconnect
  GET/POST /api/fyers/postback/config      POST /api/fyers/postback
Market data / search / options
  GET  /api/market/history             GET  /api/market/indices
  POST /api/search/refresh             GET  /api/search/symbols  /api/search/option-chain
  GET  /api/options/chain
Orders / positions / risk
  POST /api/orders   /api/orders/cancel   GET /api/orders/pending  /api/orders/quote
  GET  /api/positions/managed          POST /api/positions/{symbol}/close
  POST /api/positions/{symbol}/levels  POST /api/positions/close-all
  GET  /api/risk/state                 POST /api/risk/kill   POST /api/risk/resume
Core reads / analyses
  GET  /api/announcements/recent  /api/analyses/recent  /api/signals/recent
  GET  /api/positions  /api/trades  /api/dashboard/summary
  POST /api/analyses/run/{announcement_id}       POST /api/analyses/backfill
Observability / outcomes
  GET  /api/metrics/pipeline-latency   — p50/p95/p99 timings, news-age
       histogram, deadline hit rate, fast-track vs LLM split, sparkline series
  GET  /api/metrics/health-report      POST /api/metrics/health-report/send
  GET  /api/outcomes  /api/outcomes/summary  /api/outcomes/export.csv
  GET  /api/dataset            — the joined, column-selectable ML training set
                                 (source=all|signal|shadow, dedup, filters)
  GET  /api/dataset/columns    — column catalog (meta|feature|target roles)
  GET  /api/dataset/stats      — coverage + label balance + source split
  GET  /api/dataset/health     — per-column null rate / stats / target
                                 correlation with look-ahead-leak warnings
  GET  /api/dataset/export     — CSV / JSONL download; dedup cross-listings;
                                 split=train|val chronological time split
  POST /api/dataset/backfill   — one enrichment batch now; ?full=true loops
                                 in the background until history is complete;
                                 ?retry_failed resets stuck rows; ?rebuild
                                 re-enriches stale-schema complete rows
  GET  /api/dataset/backfill/status — full-backfill progress + pending counts
  GET  /api/risk/breaker-history       — every breaker trip / kill / resume
Notifications / webhooks / audit / backtest
  GET/POST /api/notifications/channels  PUT/DELETE .../{id}  POST .../{id}/test
  GET/POST /api/webhooks  PUT/DELETE /api/webhooks/{id}  GET /api/webhooks/{id}/deliveries
  POST /api/webhooks/in/{id}           GET /api/audit-log
  GET  /api/backtest/runs  POST /api/backtest/runs  GET/DELETE /api/backtest/runs/{id}
  GET  /api/backtest/runs/{id}/trades  /api/backtest/runs/{id}/equity-curve
WebSocket
  WS   /ws   — channels: signals, trades, positions, quotes, breaker.tripped, …

Static SPA: any non-API path serves frontend/dist/index.html (when built).

--------------------------------------------------------------------------------
17. DATA MODEL (app/db/models.py + infra_models.py) — 21 tables
--------------------------------------------------------------------------------
Core
  announcements      Raw NSE/BSE filings; UNIQUE content_hash for dedupe.
  analyses           DeepSeek/fast-track output; sentiment/score/confidence/reco,
                     key numbers, raw_response JSON (timings, tokens, cost,
                     pdf_extraction provenance). Placeholder rows tag skips
                     (stale_news, non_material_filing, ai_analysis_disabled,
                     llm_timeout, deepseek_error, invalid_json, no_prompt_template).
  signals            BUY/SELL/HOLD/BLOCK + confidence + position_size_pct;
                     entry/SL/target/RR packed into rationale; status pending|blocked.
  signal_outcomes    Passive telemetry: price at signal, +5m, +30m and % moves for
                     EVERY signal (approved AND blocked) + data-quality note. The
                     future ML training set (loose signal_id, no FK).
  dataset_features   ML enrichment (loose outcome_id/announcement_id/signal_id,
                     no FK): 1-minute-candle reaction features (T+1..T+5),
                     horizon targets (T+15) and context features (session
                     clock, NIFTY move) in a `features` JSON blob; status
                     complete|partial|no_candles|too_old|error + attempts.
                     TWO row sources: signal rows (outcome_id set) and SHADOW
                     rows (announcement_id set — analyzed filings that never
                     signaled; the negative training examples). Written by
                     the DatasetBuilder; served by /api/dataset.
  trades             Executed orders; side/qty/price/order_type/status, broker_order_id,
                     pnl, slippage_pct, r_multiple.
  positions          Current open positions (unique symbol); avg/last price,
                     unrealized_pnl, persisted stop_loss/target for re-arm.
  risk_events        Blocked-trade / risk incidents (severity, context, halted).
  risk_state         Singleton (id=1): kill switch, halted_until, current_risk_pct,
                     day/week/month equity anchors — backs the circuit breakers.
Advanced
  strategies         Named risk bundles; enabled flag; config JSON (per-strategy
                     risk overrides). Owns signal_rules.
  signal_rules       Operator logic rows: conditions JSON, action, action_params,
                     priority, enabled.
  broker_accounts    Fyers creds (app_id/secret/token — never logged), paper_mode,
                     enabled.
  prompt_templates   LLM prompts per event_type (+DEFAULT); model, temperature,
                     max_tokens, reasoning_effort, thinking_enabled, stream, version.
  prompt_history     Versioned snapshots for restore.
  webhooks           direction in|out, event_filter, url, secret, enabled.
  webhook_deliveries Per-attempt log (status_code, response_body, error).
  notification_channels  kind, config JSON, events_filter, enabled.
  notification_log   Per-send log (sent|failed|skipped).
  backtest_runs      Range, initial_capital, status, config + results JSON blob.
  audit_log          actor (ui|api|system), action, target, before/after JSON.
Infrastructure
  app_settings       key/value store backing /api/settings (UI overrides that
                     survive restart; applied into env on startup).

--------------------------------------------------------------------------------
18. CONFIGURATION & SETTINGS (app/config.py + .env + UI overrides)
--------------------------------------------------------------------------------
Precedence: process env / .env → overridden by UI settings stored in
`app_settings`, which are re-applied into the env on startup so saved tweaks
survive a restart. Never read os.environ directly — depend on get_settings().

IMPORTANT DRIFT: the shipped `.env.example` template and the code defaults in
`config.py` differ. The code default is what runs when a key is absent from
`.env`; `.env.example` is only a starting template. Notable examples:

  Parameter                 config.py default   .env.example
  POLL_INTERVAL_SECONDS     1                   2
  MAX_NEWS_AGE_SECONDS      45                  90
  MAX_HOLD_SECONDS          1080 (18 min)       1800 (30 min)
  LLM_MAX_TOKENS            300                 1024
  MAX_CAPITAL_RISK_PCT      0.75                1.0
  DAILY_MAX_LOSS_PCT        2.5                 2.0
  MAX_CONCURRENT_POSITIONS  3                   5
  LLM_TIMEOUT_SECONDS       12.0                (not in template)

Selected parameters (config.py is authoritative):
  Trading:   TRADING_MODE, PORTFOLIO_VALUE (1,000,000), DEFAULT_SL_PCT (6.0),
             DEFAULT_TARGET_RR (3.0), QUOTE_REFRESH_SECONDS (5).
  Secrets:   DEEPSEEK_API_KEY, FYERS_APP_ID/SECRET_KEY/ACCESS_TOKEN/REDIRECT_URI,
             FYERS_POSTBACK_SECRET.
  Freshness/hold: MAX_NEWS_AGE_SECONDS, MAX_HOLD_SECONDS, PIPELINE_DEADLINE_SECONDS(0).
  Risk caps: MAX_CAPITAL_RISK_PCT, DAILY_MAX_LOSS_PCT, WEEKLY_MAX_LOSS_PCT (5.0),
             MONTHLY_MAX_DRAWDOWN_PCT (8.0), MAX_CONCURRENT_POSITIONS,
             MAX_SINGLE_POSITION_PCT (20.0), MIN_LIQUIDITY_CRORE (5.0),
             MAX_SIGNALS_PER_DAY (20), MAX_TRADES_PER_DAY (12),
             MAX_CONSECUTIVE_LOSERS (4), MIN_SENTIMENT_CONFIDENCE (0.7).
  Sizing:    INTRADAY_LEVERAGE (5.0), RISK_RAMP_TRADES (100), RISK_RAMP_START_PCT (0.5).
  Stops:     SCALE_OUT_ENABLED, SCALE_OUT_R (2.0), TRAIL_ACTIVATE_R (1.5),
             TRAIL_DISTANCE_R (0.5), DEFAULT_SL_MIN_PCT (1.0),
             DEFAULT_SL_SMALLCAP_PCT (1.5), SMALLCAP_PRICE (200), ATR_ENABLED,
             ATR_PERIOD (14), ATR_STOP_MULT (2.0), ATR_MAX_STOP_PCT (8.0).
  Clustering: SECTOR_CONCENTRATION_PCT (25), MAX_POSITIONS_PER_SECTOR (2),
             SECTOR_CLUSTER_WINDOW_SECONDS (120).
  Execution: ENTRY_BUFFER_PCT (0.2), MAX_SLIPPAGE_PCT (0.25),
             ORDER_FILL_TIMEOUT_SECONDS (1.5 — the REST poll mark),
             MAX_SPREAD_PCT (0.1), INDIA_VIX_MAX (30), SHORTING_ENABLED,
             REQUIRE_KNOWN_LIQUIDITY (False).
  Entry state machine (§5b): ENTRY_MAX_DRIFT_PCT (1.5),
             RETRACEMENT_WINDOW_SECONDS (120), RETRACEMENT_POLL_SECONDS (1.0),
             SYMBOL_LOCK_WAIT_SECONDS (0.5), ENTRY_FILL_TIMEOUT_SECONDS (2.0),
             BROKER_ORDER_TIMEOUT_SECONDS (3.0).
  Exit execution (§5c): EXIT_BUFFER_PCT (0.2), EXIT_FILL_TIMEOUT_SECONDS (2.0),
             EXIT_BROKER_TIMEOUT_SECONDS (3.0), EXIT_RETRY_MAX (3).
  Exit rules (§3b): BREAKEVEN_ENABLED, BREAKEVEN_AT_PCT (2.0),
             BREAKEVEN_LOCK_PCT (0.2), CONSOLIDATION_EXIT_ENABLED,
             CONSOLIDATION_WINDOW_SECONDS (120), CONSOLIDATION_RANGE_PCT (0.5),
             CONSOLIDATION_MIN/MAX_PROFIT_PCT (1.0 / 2.5), STALL_EXIT_ENABLED,
             STALL_WINDOW_SECONDS (90), STALL_ROC_PCT (0.3),
             STALL_MIN/MAX_PROFIT_PCT (3.0 / 6.0).
  Exit safety: STALE_QUOTE_SECONDS (5.0), POSITION_RECONCILE_SECONDS (60).
  Perf sizer: PERF_SIZER_ENABLED, PERF_SIZER_MIN_TRADES (10),
             PERF_SIZER_LOOKBACK_TRADES (200), HIGH_WIN_RATE (0.60) /
             HIGH_AVG_R (1.5) / HIGH_RISK_PCT (1.0), LOW_WIN_RATE (0.50) /
             LOW_AVG_R (1.0) / LOW_RISK_PCT (0.5).
  Sentiment decay: SENTIMENT_DECAY_ENABLED, FULL_SECONDS (2.0),
             PARTIAL_SECONDS (5.0), PARTIAL_MULT (0.8), STALE_MULT (0.6).
  Health report: HEALTH_REPORT_ENABLED, HEALTH_REPORT_TIME_IST ("15:45").
  Feature flags: AI_ANALYSIS_ENABLED, FAST_TRACK_ENABLED, SEND_EXTRACTED_TEXT,
             PRE_LLM_FILTER_ENABLED, OUTCOME_LOGGER_ENABLED, FYERS_STREAMING_ENABLED.
  LLM:       LLM_TIMEOUT_SECONDS, LLM_MAX_TOKENS, LLM_MAX_RETRIES (1),
             LLM_RETRY_BACKOFF_SECONDS (0.5).
  PDF:       PDF_FETCH_TIMEOUT_SECONDS (4.0), PDF_MAX_TEXT_CHARS (24,000).
  Session (IST): MARKET_OPEN 09:15, MARKET_CLOSE 15:30, ENTRY_WINDOW 09:30–15:00,
             SQUARE_OFF_TIME 15:10, ENFORCE_MARKET_HOURS.
  Server:    BIND_HOST 127.0.0.1, BIND_PORT 8000, LOG_LEVEL INFO, DATABASE_URL,
             TESTING (1 → in-memory SQLite).
Startup warns loudly if bound to a non-loopback interface (no auth).

--------------------------------------------------------------------------------
19. THE FRONTEND (frontend/, React 18 + Vite 6)
--------------------------------------------------------------------------------
Bloomberg-terminal-styled SPA. Hash router (router.tsx), lazy-loaded pages, a
grouped sidebar (collapsible, state persisted in localStorage), and a bottom
status bar (WS state, MODE, CAP, live NSE index ticker over /ws, IST clock).
Theme-aware (themes.ts, useTheme; data-theme on <html>). Server state via
TanStack Query (hooks/useApi.ts); realtime via useWebSocket / useQuotes.

Nav (App.tsx), grouped MAIN / OPS / CFG:
  MAIN: Dashboard, Trade, Trade History, Outcomes, Dataset, Prompts, Rules,
        Strategies
  OPS:  Backtest, Accounts, Notifications, Webhooks
  CFG:  Settings

Pages (src/pages/): Dashboard, Trade, TradeHistory, Outcomes, Dataset, Prompts,
Rules, Strategies, Accounts, Notifications, Webhooks, Backtest, Settings.

Trade page (src/pages/Trade.tsx, ~1050 lines): the operator-driven manual order
ticket. REAL-MONEY ONLY — paper-mode accounts are filtered out of the broker
picker (the backend also rejects them with 400). Layout (Bloomberg-style,
dense, dark): search bar → quote panel + order ticket + pending orders +
positions + option chain + interactive chart. Features:
  • Symbol search: autocomplete powered by the instrument master (Fyers scrip-
    master CSVs — NSE/BSE cash + F&O). POST /api/search/refresh downloads the
    full master on demand.
  • Quote panel: real-time LTP, bid/ask, volume from Fyers (/api/orders/quote
    + /ws live overlay).
  • Order ticket: BUY/SELL, qty, order type (MARKET/LIMIT/SL/SL-M), product
    (MIS/CNC/NRML), optional limit/stop price. Risk engine is consulted; on
    block the ticket surfaces the blocking codes and a "BYPASS" field requiring
    the operator to type "I ACCEPT THE RISK" before re-submitting.
  • Pending orders panel: lists PENDING trades from /api/orders/pending with
    per-row cancel.
  • Positions panel: live open positions with real-time P&L (overlays /ws
    ticks over the 5s REST poll); qty is signed (negative for shorts).
  • Option chain panel: renders when the selected symbol is an index
    underlying. Live chain from Fyers /data/options-chain-v3 (per-strike CE/PE
    with LTP, bid/ask, OI, expiry picker); falls back to the static
    instrument master when Fyers isn't connected. GET /api/options/chain.
  • ChartPanel (src/components/trade/ChartPanel.tsx, ~2400 lines): TradingView-
    style interactive chart built on lightweight-charts v5. Candles from
    GET /api/market/history (Fyers-only), last bar kept live from /ws.
      Timeframes: 1m, 3m, 5m, 15m, 30m, 1h, 1D.
      Chart types: candles, bars, line, area.
      Volume histogram + bar-close countdown timer.
      Overlays: SMA, EMA, WMA, VWAP, Bollinger Bands, Supertrend, Parabolic
        SAR, Ichimoku Cloud, Donchian Channel.
      Oscillator panes: RSI, MACD, Stochastic, ADX/DI, ATR, OBV, CCI, MFI,
        Williams %R.
      Drawing tools (src/components/trade/drawings.ts): trend line, ray,
        horizontal/vertical line, rectangle, fib retracement, text note —
        click-select + Delete, persisted per symbol in localStorage.
      Price alerts: click a price, fires on live-tick cross (toast + browser
        notification), persisted per symbol.
      Compare symbols on a percentage scale.
      Price-scale modes (log / percent), crosshair magnet, PNG screenshot,
        fullscreen, OHLC legend, infinite scroll-back pagination.
      Times: all Fyers epoch timestamps shifted +05:30 for IST labels.
  Technical indicators (src/lib/indicators.ts, ~19KB): pure-function
    implementations for all overlay and oscillator calculations.

Accounts page (src/pages/Accounts.tsx): intentionally minimal. Two accounts:
  1. Paper Trading — built-in, always available, simulated fills.
  2. Fyers — connect in one click via OAuth (keys live in .env).
  No broker-account CRUD; just connect Fyers. Components:
    ConnectFyers (src/components/accounts/ConnectFyers.tsx): one-click OAuth
      banner, pre-validates the app_id against Fyers, CSRF state management.
    FyersCredentialsCard: shows/masks FYERS_APP_ID and SECRET_KEY from .env.
    FyersSetupCard: step-by-step Fyers API app activation guide with the exact
      redirect URI to paste into the Fyers dashboard.
    FyersWebhookCard: postback webhook registration status + secret.

Outcomes page (Phase 4 complete): what every signal's stock ACTUALLY did
(+5m/+30m moves, AI-verdict correctness, taken vs blocked), a per-event-type
performance chart + table, an event filter, and a CSV export for offline ML.
Dataset page: the ML training-set builder UI: coverage tiles (rows split
signal+shadow, enrichment %, label balance), a grouped column picker where
every column carries its FEATURE (safe input) / TARGET (label only — look-
ahead bias as an input) / META badge, ready-made presets (everything, Model A
direction, Model B peak, features only), filters (source, event, action,
taken/blocked, label, enriched-only), a collapsible column-health panel (null
rates, stats, target correlation, ⚠ LEAK flags), a live preview table,
CSV/JSONL export plus one-click chronological Train/Val CSVs with a dedup-
cross-listings toggle (default ON), and an "Enrich now" backfill button
(column selection persists in localStorage `tradebot.dataset.columns.v1`).

Dashboard components: AccountToggles (Paper / Fyers on-off), AiAnalysisToggle,
NewsPipeline (scrape→analyze→signal table), ActivePositions, PipelineLatency
(p95 headline + total-latency sparkline, LLM p95, fast-track %, deadline hit
rate, news-age p95 — backed by /api/metrics/pipeline-latency), PnLChart,
RiskMetrics, Skeleton. Settings page additionally hosts BreakerHistory (every
breaker trip with threshold/value/action) beside the AuditLog. Backtest uses
recharts (EquityCurveChart).

PnLChart is customizable: time range (7D/14D/30D/90D), view (cumulative area /
daily bars, sign-colored), and account scope (All / Paper / Live — simulated
P&L never pollutes the real-money read). Choices persist in localStorage
(`dashboard.pnl.prefs`). It computes realised P&L from /api/trades with the
SAME FIFO round-trip logic as Trade History (`lib/roundtrips.ts`), seeding the
cumulative line with pre-window P&L so its last point equals the all-time
realised total; longer ranges widen the trades fetch (500→1000 rows).

Operational controls live in the UI, mostly on the Dashboard (memory:
"Frontend-only control"). The Settings page currently hosts: GlobalSettings
(risk / speed / feature-flag editors), ThemeSwitcher, and the AuditLog. NOTE: a
standalone Trading Mode (Paper/LIVE) toggle component exists
(components/settings/TradingModeToggle.tsx) but was REMOVED from the Settings
page in this session and is no longer mounted; mode switching happens via the
API and the Dashboard/Fyers toggles. Live flips require typing "LIVE" to confirm.

Frontend tests (vitest, src/tests/): AccountToggles, ConnectFyers, Dashboard,
Prompts, Trade, indicators, useWebSocket.

--------------------------------------------------------------------------------
20. IMPROVEMENT ROADMAP (plan.txt)
--------------------------------------------------------------------------------
Constraint: never destroy existing behavior — every new feature ships behind a
frontend toggle defaulting to current behavior; all controls in the UI.
  Phase 0 [DONE]     Per-stage latency instrumentation (analyzer.pipeline_timing:
                     pdf_fetch_ms / pdf_extract_ms / llm_ms / total_ms /
                     news_age_s_at_start on analyses.raw_response.timings).
  Phase 1 [DONE]     Real filing text into the pipeline (PDF prefetch, PyMuPDF
                     extraction, Hindi filter, page scoring, scanned-PDF fallback,
                     extracted-text toggle with NO-TRADE-by-default footer).
  Phase 2 [DONE*]    Two-track signals: deterministic fast track + hybrid PDF
                     value parse. (*XBRL results parser DEFERRED — needs live
                     XBRL URLs + taxonomy mapping built against real filings.)
  Phase 3 [DONE]     LLM track under target latency: token cap, 1 fast retry,
                     hard pipeline deadline, editable max news age. Measured
                     2026-07-04 ~2.5–2.7s end-to-end off-peak; re-check at market
                     hours via the timing logs.
  Phase 4 [DONE]     Passive outcome logger + signal_outcomes table, AND the
                     review/export view: the Outcomes page + /api/outcomes
                     (+ summary + CSV export) close the feedback loop.
  Phase 5 [BLOCKED]  Local materiality pre-filter (FinBERT/IndicBERT-family)
                     to kill routine noise before any LLM call. Needs ~1–2k
                     logged outcomes first (memory: "ML phases blocked on data" —
                     backfill from historical candles is the unblock).
  Phase 6 [BLOCKED]  Fine-tune an LLM for fact/event extraction (not buy/sell
                     direction; the rules engine stays the decision-maker).
  Ongoing            Dashboard latency + hit-rate tiles — DONE (Pipeline
                     Latency tile; hit rate on the Outcomes page).

  Roadmap 2 (plan.txt, added 2026-07-10) — observability & strategy
  intelligence. BUILT: pipeline-latency metrics + tile; daily health report
  (system.report → notifications); performance-weighted sizer; sentiment
  decay curve; Signal Outcomes viewer; circuit-breaker history. DEFERRED
  (need new data infra; design notes in plan.txt): trade replay simulator
  (tick history), sector momentum overlay + market regime indicator (rule
  fields fed by index samplers), multi-strategy sandbox, alpha capture ratio.

--------------------------------------------------------------------------------
21. SETUP & EXECUTION GUIDE
--------------------------------------------------------------------------------
Prerequisites: Python 3.11, Node.js 20+.

First boot (local paper mode), Windows PowerShell:
  1. cd "C:\Projects\AI News Trading Bot"
  2. python -m venv .venv
  3. .\.venv\Scripts\pip install -r requirements.txt
  4. copy .env.example .env      # then edit DEEPSEEK_API_KEY (and FYERS_* for live)
  5. .\scripts\run.ps1           # activates venv, seeds prompts, builds UI, runs uvicorn
  6. open http://127.0.0.1:8000/

Developer mode (two terminals):
  Term 1: .\scripts\dev.ps1      # uvicorn --reload on 127.0.0.1:8000
  Term 2: cd frontend ; npm install ; npm run dev  # Vite HMR on http://localhost:5173/
  (POSIX equivalents: scripts/run.sh, scripts/dev.sh.)

CAUTION (memory: "Uvicorn reload wedge"): dev.ps1 runs uvicorn --reload watching
app/; editing backend files can wedge it on open /ws connections. Probe :8000
after backend edits and restart detached if it hangs.

Going live (Fyers):
  1. Create a Fyers API app at https://myapi.fyers.in/dashboard/.
  2. Set FYERS_APP_ID / FYERS_SECRET_KEY / FYERS_REDIRECT_URI in .env.
  3. Authenticate: python scripts/setup_fyers.py  (OAuth token flow).
  4. Register the postback webhook: python scripts/register_fyers_webhook.py
     (needs a public tunnel, e.g. cloudflared / scripts/start_webhook_tunnel.ps1).
  5. Add/enable the live broker account via the Accounts page.
  6. Switch TRADING_MODE to live in the UI (type "LIVE" to confirm). No file to
     create — the old `.i_accept_live_risk` sentinel is gone.
See memory "Fyers .env single-source & SEBI dual-dashboard" for the
app-id/secret/token flow and the activate→disconnect→reconnect sequence that
fixes orders rejecting as the old app.

--------------------------------------------------------------------------------
22. SCRIPTS (scripts/)
--------------------------------------------------------------------------------
  run.ps1 / run.sh                 Production single-process launch (builds UI).
  dev.ps1 / dev.sh                 Dev launch (uvicorn --reload; Vite in term 2).
  setup_fyers.py                   Full Fyers setup automation (~20KB): OAuth
                                   token flow, tunnel auto-detection (cloudflared
                                   / ngrok), webhook registration, status command.
                                   Called from the Accounts page or CLI.
  register_fyers_webhook.py        Register the Fyers postback webhook + secret.
  start_webhook_tunnel.ps1         Start a cloudflared tunnel for inbound webhooks.
  fix_fyers_app_id.py              Repair a mismatched Fyers app id on orders.
  seed_default_prompts.py          Seed/refresh DEFAULT prompt templates.
  seed_default_rules.py            Optional: re-seed the starter rule set on demand.
  seed_demo_data.py                Seed demo data for the UI.
  backtest_seed.py                 Seed announcements for backtesting.
  clear_trading_data.py            Wipe trading data (dev reset).
  fix_doubled_trades.py            Data-repair utility.
  smoke_e2e.py, smoke_t2_monitor.py, smoke_t3_analyzer.py, smoke_t4_execution.py
                                   Track-by-track smoke tests.
  _oauth.py, _tunnel.py, _debug_smoke.py   Internal helpers (OAuth flow,
                                   tunnel management, debug utilities).

--------------------------------------------------------------------------------
23. TESTING
--------------------------------------------------------------------------------
Backend: 61 pytest modules / ~900 tests under app/tests/ (+ conftest.py)
covering monitors, dedup, analyzer/service, schemas, fast track, rules, risk
engine + API, circuit breakers, position sizer, volatility, event profiles,
market clock/data/history, paper backend, IOC execution, trade exits/manager,
Fyers auth/live/stream/postback/webhook/data-decoupled, notifications
(telegram/discord/email), webhooks in/out + signing, trading-mode API +
routing, backtest engine + API, metrics, prompts + seeding, settings API, e2e,
phase-3 latency, hybrid + outcomes, dataset, and more. Key test suites:
  test_entry_state_machine.py  E-01..E-15 — the entry plan's definitive cases
      (happy path, drift + retracement re-arm/expiry, partial/zero fill,
      slippage breach, broker rejection/timeout, symbol-lock concurrency,
      flash crash during the fill window) + Manager integration + WS/postback
      fill dedup.
  test_exit_management.py      T-01..T-30 — the exit plan's definitive cases
      (marketable-limit exits, breakeven, time/consolidation/stall exits,
      trailing activate/ratchet/trigger, gap fallback, exit partial fills,
      rejection escalation, stop-over-target precedence, restart recovery,
      CLOSED_EXTERNAL reconciliation, stale-feed guard, stop/target
      validation, sizing edges) + a realtime tick-driven exit test. Run these
      whenever trade_manager.py or entry_manager.py changes.
  test_observability.py        pipeline-latency metrics, health report,
      performance-weighted sizer (tiers + engine integration), sentiment
      decay, outcomes API (+ summary + CSV), breaker history.
  test_trade_page.py           instrument master (seed, search, option chain),
      symbol search API, option chain endpoint, manual order placement (happy
      path, paper-account rejection, validation, risk-block + bypass), cancel,
      pending orders, quote polling.
  test_setup_automation.py     Fyers setup automation: tunnel backend
      detection (cloudflared/ngrok), URL extraction from process output, OAuth
      flow polling, status command — all monkey-patched, no real binaries.
Run with TESTING=1 (in-memory SQLite): `pytest` (or `python -m pytest`).
Frontend: 7 vitest suites / 56 tests under frontend/src/tests/ — `npm test`.

--------------------------------------------------------------------------------
24. TROUBLESHOOTING
--------------------------------------------------------------------------------
- Blank UI / dashboard missing: frontend/dist wasn't built. run.ps1 builds it;
  in dev mode use the Vite server on :5173/:5174.
- Fyers login error -50: your public IP isn't whitelisted in the Fyers API
  dashboard — update it.
- Orders reject as the wrong app: Fyers app-id/token mismatch — see the memory
  note and scripts/fix_fyers_app_id.py; follow activate→disconnect→reconnect.
- LLM timeouts: DeepSeek congested — check LLM_TIMEOUT_SECONDS and the
  analyzer.pipeline_timing logs; the analyzer retries once then discards.
- No trades in live mode: confirm TRADING_MODE=live, a live Fyers account is
  connected + enabled, the market is open (or ENFORCE_MARKET_HOURS is off for
  testing), and no circuit breaker / risk gate is blocking (VIX, spread,
  concurrency, sector, daily loss). Check /api/risk/state.
- "NO_LIVE_PRICE" / blocked entries: the live feed is cold — by design the system
  blocks rather than filling at a synthetic price.
- Backend wedged after an edit: uvicorn --reload can hang on open /ws — probe
  :8000 and restart detached.

--------------------------------------------------------------------------------
25. DESIGN PHILOSOPHY & INVARIANTS
--------------------------------------------------------------------------------
• No black boxes: the operator curates the logic (prompts + rules); the bot
  provides the framework and the guardrails.
• Latency is the product: fail fast; a late entry is worse than no entry.
• Early entry is the edge: never fix whipsaw by delaying entry — fix the stop,
  the sizing, and the slippage caps.
• Intraday only: no delivery, no overnight carry; TIME_EXIT + EOD square-off
  always flatten.
• Fyers-only pricing: no public-feed fallback; a cold feed blocks, never fills
  at a synthetic price; entry and exit share one price source.
• Fail-safe defaults: missing data → skip/no-trade (or block in live for
  liquidity), never a faked pass.
• Non-bypassable risk: every signal goes through the risk engine + circuit
  breakers; no override flag exists.
• Never retry blind: a broker call that times out MAY have placed the order —
  entries are never re-fired (BROKER_TIMEOUT is terminal), and exit chases are
  explicit market orders for known remainders, never duplicates.
• Realtime exits, fresh data only: stop/target/trailing react on every Fyers
  WS tick, but a price-triggered exit requires a tick younger than
  STALE_QUOTE_SECONDS — a frozen feed can never fire an exit at a stale price.
  Time-based exits (and the EOD square-off) always run.
• The broker is the source of truth: live entries hand over the broker's exact
  fill (qty + price); live exits settle only on broker confirmation; the
  reconciliation loop syncs externally-closed positions (CLOSED_EXTERNAL)
  instead of ghost-managing them. Duplicate fill confirmations (order-WS,
  postback, REST poll) converge on one trade row, applied once.
• Protective exits always win: the hard stop is checked before the target on
  every tick; a fill through the limit or through the stop is flattened at
  market immediately (SLIPPAGE_BREACH / FLASH_CRASH_EXIT); a failed exit
  escalates to the operator but keeps retrying — a position is never silently
  abandoned.
• Frontend-only control: every operational switch lives in the UI; no file
  sentinels, no CLI gates. Going live needs a typed "LIVE" confirmation.
• Non-destructive evolution: new pipeline features ship behind toggles that
  default to the existing behavior; saved prompt edits and deleted rules survive
  restarts (no silent re-seeding).

================================================================================
END OF FILE — AI News Trading Bot
================================================================================